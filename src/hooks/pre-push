#!/usr/bin/env bash
# agent-workflow — installed via core.hooksPath. Blocks pushing the default branch when a
# merged story has no passed review. Reads <local ref> <local sha> <remote ref> <remote sha> on stdin.
set -euo pipefail
exec "$(dirname "$0")/aw-gate.sh" pre-push
